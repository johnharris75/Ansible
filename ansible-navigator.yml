ansible-navigator:
  execution-environment:
    container-engine: podman
    image: quay.io/ansible/creator-ee
    pull:
      policy: missing
  playbook-artifact: 
    enable: false

  logging:
    level: info
    append: true
    file: /home/devops/ansible/ansible-navigator.log
