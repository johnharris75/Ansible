---
- name: cron
  become: true
  hosts: all
  tasks:
    - name: cron job
      ansible.builtin.cron:
        name: cron as nat
        user: nat
        minute: "*/2"
        job: logger "ex294"
        state: present
