---
- name: genhost
  hosts: all
  become: true
  tasks:
    - name: template
      ansible.builtin.template:
        src: ./hosts.j2
        dest: /etc/myhosts
        mode: '0644'
        owner: root
        group: root
      when: inventory_hostname in groups['dev']
